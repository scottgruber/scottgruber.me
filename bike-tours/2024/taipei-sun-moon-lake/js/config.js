// GENERATED FILE — edit eleventy.config.js's mapboxToken global data / .env,
// not this file directly (it's rendered from src/config.njk).
//
// MAPBOX_TOKEN must be a PUBLIC token — it starts with "pk." and is visible
// to anyone who views source, which is fine and expected so long as it is
// URL-restricted to this site's domain in the Mapbox account.
//
// If empty, maps fall back to CARTO's keyless tiles, so the site still
// builds and renders — just on the rate-limited basemap.
window.TSML_CONFIG = {
  MAPBOX_TOKEN: "pk.eyJ1Ijoic2NvdHRncnViZXIiLCJhIjoiY210ZngzdXdqMHpnbDJ5cHQ1NWdwcm45OCJ9.rLX-CrCt3RimnjdZwlLJ_Q",
};
